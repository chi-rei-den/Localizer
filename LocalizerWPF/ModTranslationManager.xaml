<UserControl x:Class="LocalizerWPF.ModTranslationManager"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:local="clr-namespace:LocalizerWPF"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <DataGrid x:Name="DataGrid" SelectionMode="Single" SelectedIndex="0" Grid.Row="0" AutoGenerateColumns="False">
        <DataGrid.Columns>
            <DataGridTextColumn Header="Mod" Binding="{Binding Path=Key}" />
            <DataGridTextColumn Header="Version" Binding="{Binding Path=Version}" />
            <DataGridTextColumn Header="Count" Binding="{Binding Path=Value.Count}" />
        </DataGrid.Columns>
        <DataGrid.RowDetailsTemplate>
            <DataTemplate>
                <Grid>
                    <Grid.Resources>
                        <DataTemplate x:Key="EnabledSwitchTemplate">
                            <Grid>
                                <Controls:ToggleSwitch IsChecked="{Binding Path=Enabled, Mode=TwoWay}"/>
                            </Grid>
                        </DataTemplate>
                    </Grid.Resources>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="{Binding Path=Key}"></TextBlock>
                        <TextBlock Text="Mod Description"></TextBlock>
                        <Button Margin="4,4,4,4">
                            <TextBlock Text="Move Up"></TextBlock>
                        </Button>
                        <Button Margin="4,4,4,4">
                            <TextBlock Text="Move Down"></TextBlock>
                        </Button>
                        <Button Margin="4,4,4,4">
                            <TextBlock Text="Move To Top"></TextBlock>
                        </Button>
                        <Button Margin="4,4,4,4">
                            <TextBlock Text="Move To Bottom"></TextBlock>
                        </Button>
                    </StackPanel>
                    <DataGrid ItemsSource="{Binding Path=Value}"
                              SelectionMode="Single" SelectedIndex="0" Grid.Column="1" AutoGenerateColumns="False">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn CellTemplate="{StaticResource EnabledSwitchTemplate}" />
                            <DataGridTextColumn Header="Name" Binding="{Binding Path=Name}" />
                            <DataGridTextColumn Header="Author" Binding="{Binding  Path=Author}" />
                            <DataGridTextColumn Header="Version" Binding="{Binding Path=Version}" />
                            <DataGridTextColumn Header="Count" Binding="{Binding Path=Translations.Count}" />
                            <DataGridTextColumn Header="Language" Binding="{Binding Path=Language.NativeName}" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </DataTemplate>
        </DataGrid.RowDetailsTemplate>
    </DataGrid>
</UserControl>
